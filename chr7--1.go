slices == arrays++
append(slicename, plusVar)  === to add something into slice
plusVar == { , , , 'here the new'}
slice := []int{}  == can used default by prog
slice 1 := make([]int, 6, 2) === to create ana slice or array
slice2 := []int(1,2,3,4)
slice1 = append(slice1, slice2)
